/*
 ============================================================================
 Name        : Ejercicio 1-2.c
 Author      : Ornela Curcio
 Version     :
 Copyright   :
 Description : ingresar 3 números y mostrar el mayor de los tres.
 ============================================================================
 */

#include <stdio.h>
#include <stdlib.h>

int main(void) {


	setbuf(stdout, NULL);

	 	int contadorNumero;
		int numeroIngresado;
		int numeroMayor;

		for(contadorNumero=0; contadorNumero<3; contadorNumero++)
		{
			printf("Ingrese un numero");

			scanf("%d", &numeroIngresado);

			if(numeroIngresado==1)
			{
				numeroMayor=numeroIngresado;
			}
			else
			{
				if(numeroIngresado>numeroMayor)
				{
					numeroMayor=numeroIngresado;
				}
			}
		}

		printf("El mayor numero de los 3 es %d", numeroMayor);




		return EXIT_SUCCESS;

}
